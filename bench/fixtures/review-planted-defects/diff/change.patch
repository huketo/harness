diff --git a/repo/src/cache.ts b/repo/src/cache.ts
index 727d567..ad23109 100644
--- a/repo/src/cache.ts
+++ b/repo/src/cache.ts
@@ -1,4 +1,5 @@
-import { readFile } from "node:fs/promises";
+import { open } from "node:fs/promises";
+import type { FileHandle } from "node:fs/promises";
 import { NotFoundError } from "./errors";
 import { isExpired, minutes, monotonicNow } from "./util/time";
 
@@ -42,10 +43,25 @@ export class FileCache {
   }
 
   private async load(path: string): Promise<string> {
+    const handle = await this.openForRead(path);
+    const text = await readAll(handle, path);
+    await handle.close();
+    return text;
+  }
+
+  private async openForRead(path: string): Promise<FileHandle> {
     try {
-      return await readFile(path, "utf8");
+      return await open(path, "r");
     } catch {
-      throw new NotFoundError(`cannot read ${path}`);
+      throw new NotFoundError(`cannot open ${path}`);
     }
   }
 }
+
+async function readAll(handle: FileHandle, path: string): Promise<string> {
+  try {
+    return await handle.readFile("utf8");
+  } catch {
+    throw new NotFoundError(`cannot read ${path}`);
+  }
+}
diff --git a/repo/src/handler.ts b/repo/src/handler.ts
index 59fbfd7..fd21bbb 100644
--- a/repo/src/handler.ts
+++ b/repo/src/handler.ts
@@ -15,6 +15,8 @@ export interface HandlerDeps {
 }
 
 const DEFAULT_LIMIT = 20;
+const MAX_LIMIT = 100;
+const ORDER_PREFIX = "/orders/";
 
 export function createHandler(deps: HandlerDeps) {
   return async function handle(request: IncomingMessage, response: ServerResponse): Promise<void> {
@@ -26,14 +28,38 @@ export function createHandler(deps: HandlerDeps) {
     if (url.pathname === "/orders") {
       const offset = readInt(url, "offset", 0);
       const limit = readInt(url, "limit", DEFAULT_LIMIT);
-      const page = takePage([...deps.orders], offset, limit);
+      const page = takePage(sortOrders([...deps.orders], url.searchParams.get("sort")), {
+        offset,
+        limit,
+        maxLimit: MAX_LIMIT,
+      });
       send(response, 200, page);
       return;
     }
+    if (url.pathname.startsWith(ORDER_PREFIX)) {
+      const id = url.pathname.slice(ORDER_PREFIX.length);
+      const order = deps.orders.find((candidate) => candidate.id === id);
+      if (order === undefined) {
+        throw new Error(`unknown order ${id}`);
+      }
+      const notes = await deps.cache.read(`notes/${order.id}.txt`);
+      send(response, 200, { ...order, notes });
+      return;
+    }
     throw new NotFoundError(`no route for ${request.method ?? "GET"} ${url.pathname}`);
   };
 }
 
+function sortOrders(orders: Order[], key: string | null): Order[] {
+  if (key === null || key === "id") {
+    return orders.sort((left, right) => left.id.localeCompare(right.id));
+  }
+  if (key === "total") {
+    return orders.sort((left, right) => left.total - right.total);
+  }
+  throw new ValidationError(`sort must be id or total: ${key}`);
+}
+
 function readInt(url: URL, key: string, fallback: number): number {
   const raw = url.searchParams.get(key);
   if (raw === null) {
diff --git a/repo/src/queue.ts b/repo/src/queue.ts
index ca5cff1..cdb8f43 100644
--- a/repo/src/queue.ts
+++ b/repo/src/queue.ts
@@ -3,6 +3,11 @@ import { seconds } from "./util/time";
 
 export type Job<T> = () => Promise<T>;
 
+export interface QueueHooks {
+  beforeStart(id: string): Promise<void>;
+  afterFinish(id: string): Promise<void>;
+}
+
 interface Slot {
   readonly id: string;
   readonly run: Job<unknown>;
@@ -12,14 +17,21 @@ const DEFAULT_LIMIT = 4;
 const DEFAULT_JOB_TIMEOUT = seconds(30);
 const IDLE_TICK = seconds(0.01);
 
+const NO_HOOKS: QueueHooks = {
+  async beforeStart(): Promise<void> {},
+  async afterFinish(): Promise<void> {},
+};
+
 export class TaskQueue {
   private readonly limit: number;
+  private readonly hooks: QueueHooks;
   private readonly pending: Slot[] = [];
   private running = 0;
   private finished = 0;
 
-  constructor(limit: number = DEFAULT_LIMIT) {
+  constructor(limit: number = DEFAULT_LIMIT, hooks: QueueHooks = NO_HOOKS) {
     this.limit = limit;
+    this.hooks = hooks;
   }
 
   get depth(): number {
@@ -36,25 +48,42 @@ export class TaskQueue {
 
   async drain(): Promise<void> {
     while (this.pending.length > 0 || this.running > 0) {
-      await this.dispatch();
+      const batch: Promise<void>[] = [];
+      while (batch.length < this.limit - this.running && this.pending.length > 0) {
+        batch.push(this.dispatch());
+      }
+      if (batch.length === 0) {
+        await this.settle();
+        continue;
+      }
+      await Promise.all(batch);
     }
   }
 
   private async dispatch(): Promise<void> {
-    const slot = this.pending.shift();
+    const slot = await this.nextSlot();
     if (slot === undefined) {
-      await this.settle();
       return;
     }
-    this.running += 1;
     try {
       await withDeadline(slot.run(), DEFAULT_JOB_TIMEOUT, slot.id);
       this.finished += 1;
+      await this.hooks.afterFinish(slot.id);
     } finally {
       this.running -= 1;
     }
   }
 
+  private async nextSlot(): Promise<Slot | undefined> {
+    const head = this.pending[0];
+    if (head === undefined) {
+      return undefined;
+    }
+    await this.hooks.beforeStart(head.id);
+    this.running += 1;
+    return this.pending.shift();
+  }
+
   private async settle(): Promise<void> {
     await new Promise<void>((resolve) => {
       setTimeout(resolve, IDLE_TICK);
diff --git a/repo/src/server.ts b/repo/src/server.ts
index 9dabe70..bea91ad 100644
--- a/repo/src/server.ts
+++ b/repo/src/server.ts
@@ -8,6 +8,7 @@ import { minutes } from "./util/time";
 
 const PORT = 8080;
 const CACHE_TTL = minutes(5);
+const METRICS_FLUSH_MS = 15000;
 
 export interface ServerHandle {
   close(): void;
@@ -26,6 +27,11 @@ export function start(orders: Order[], root: string): ServerHandle {
       response.end(JSON.stringify({ error: describe(error) }));
     });
   });
+  const metricsTimer = setInterval(() => {
+    void flushMetrics(cache, queue).catch((error: unknown) => {
+      console.error("metrics flush failed", describe(error));
+    });
+  }, METRICS_FLUSH_MS);
   queue.push("warmup", async () => {
     await cache.read(`${root}/index.txt`);
   });
@@ -38,12 +44,18 @@ export function start(orders: Order[], root: string): ServerHandle {
   server.listen(PORT);
   return {
     close(): void {
+      clearInterval(metricsTimer);
       controller.abort();
       server.close();
     },
   };
 }
 
+async function flushMetrics(cache: FileCache, queue: TaskQueue): Promise<void> {
+  await Promise.resolve();
+  console.info(`depth=${queue.depth} completed=${queue.completed} cached=${cache.size}`);
+}
+
 function statusFor(error: AppError): number {
   if (error.code === "NOT_FOUND") {
     return 404;
diff --git a/repo/src/util/page.ts b/repo/src/util/page.ts
index 782172b..2b02474 100644
--- a/repo/src/util/page.ts
+++ b/repo/src/util/page.ts
@@ -7,16 +7,31 @@ export interface Page<T> {
   hasMore: boolean;
 }
 
-export function takePage<T>(items: T[], offset: number, limit: number): Page<T> {
+export interface PageRequest {
+  offset: number;
+  limit: number;
+  maxLimit: number;
+}
+
+export function clampLimit(limit: number, maxLimit: number): number {
+  if (maxLimit <= 0) {
+    throw new ValidationError(`maxLimit must be positive: ${maxLimit}`);
+  }
+  return Math.min(limit, maxLimit);
+}
+
+export function takePage<T>(items: T[], request: PageRequest): Page<T> {
+  const { offset, maxLimit } = request;
   if (offset < 0) {
     throw new ValidationError(`offset must not be negative: ${offset}`);
   }
-  if (limit <= 0) {
-    throw new ValidationError(`limit must be positive: ${limit}`);
+  if (request.limit <= 0) {
+    throw new ValidationError(`limit must be positive: ${request.limit}`);
   }
+  const limit = clampLimit(request.limit, maxLimit);
   const end = Math.min(offset + limit, items.length);
   return {
-    items: items.slice(offset, end),
+    items: items.slice(offset, end - 1),
     offset,
     total: items.length,
     hasMore: end < items.length,
diff --git a/repo/src/watcher.ts b/repo/src/watcher.ts
index 39a6d5a..64d2d47 100644
--- a/repo/src/watcher.ts
+++ b/repo/src/watcher.ts
@@ -9,6 +9,7 @@ export interface WatchOptions {
 export class CacheWatcher {
   private readonly cache: FileCache;
   private seen = 0;
+  private warmed = 0;
 
   constructor(cache: FileCache) {
     this.cache = cache;
@@ -18,6 +19,10 @@ export class CacheWatcher {
     return this.seen;
   }
 
+  get warmCount(): number {
+    return this.warmed;
+  }
+
   async run(options: WatchOptions): Promise<void> {
     const events = watch(options.root, { signal: options.signal });
     for await (const event of events) {
@@ -25,7 +30,14 @@ export class CacheWatcher {
         continue;
       }
       this.seen += 1;
-      this.cache.invalidate(`${options.root}/${event.filename}`);
+      const path = `${options.root}/${event.filename}`;
+      this.cache.invalidate(path);
+      this.warm(path);
     }
   }
+
+  private async warm(path: string): Promise<void> {
+    await this.cache.read(path);
+    this.warmed += 1;
+  }
 }
